# Read-only DSN against the Postgres owned by hr-backend (host-exposed by docker-compose).
DATABASE_URL=postgresql://hr:hr_secret@localhost:5432/hr_platform

# --- Placeholders (unused in Sprint 0) ---
EMBED_MODEL=BGE-M3
EMBED_DIM=1024
ANTHROPIC_API_KEY=
